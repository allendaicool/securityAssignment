#include <iostream>
#include <string>
#include <fstream>
#include <unistd.h>
#include <stdlib.h>
#include <ctype.h>
#include <stdio.h>
#include <getopt.h>
#include "help.h"
#include "functionCall.h"
using namespace std;

int main(int argc, const char * argv[])
{
	newFile = fopen(fileName.c_str(),"w+");
	
	if(newFile == NULL)
		exit(EXIT_FAILURE);


}
